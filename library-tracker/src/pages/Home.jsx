import { Link, useOutletContext } from 'react-router-dom'
import '../styles/Home.css'
function Home() {
  const { library, profile } = useOutletContext(); const currentlyReading = library.find((book) => book.status === 'reading'); const count = (status) => library.filter((book) => book.status === status).length
  return <main className='home-grid'><header className='home-heading'><p className='eyebrow'>A calm place for every story</p><h1>Library Tracker</h1></header><section className='reading-book' aria-label='Currently reading'>{currentlyReading ? <Link className='book-shell' to={`/reading/${currentlyReading.id}`}><div className='book-spine' /><div className='book-page'><span className='book-label'>Currently reading · Open notes</span><h2>{currentlyReading.title}</h2><p>{currentlyReading.author}</p></div></Link> : <div className='empty-state'><p>No book currently being read.</p><Link to='/shelves?status=tbr'>Choose one from your TBR shelf →</Link></div>}</section><aside className='library-card'><div className='library-card-top'><span className='card-badge'>Library card</span><Link to='/profile'>Edit</Link></div><h2>{profile.name}</h2><dl><div><dt>Age</dt><dd>{profile.age || '—'}</dd></div><div><dt>Favourite genre</dt><dd>{profile.favoriteGenre || '—'}</dd></div><div><dt>Favourite book</dt><dd>{profile.favoriteBook || '—'}</dd></div></dl><blockquote>“{profile.quote}”</blockquote></aside><section className='home-stats' aria-label='Library stats'><Link className='stat-pill tbr-pill' to='/shelves?status=tbr'><span>To be read</span><strong>{count('tbr')}</strong></Link><Link className='stat-pill' to='/cart'><span>Shopping list</span><strong>{count('wishlist')}</strong></Link><Link className='stat-pill finished-pill' to='/shelves?status=finished'><span>Finished</span><strong>{count('finished')}</strong></Link></section></main>
}
export default Home
